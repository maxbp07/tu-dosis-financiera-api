services:
  video-api:
    build: .
    environment:
      - AZURE_SPEECH_KEY=40B0QJmvMmjUQzzVVPbKIZkX2A4t1Gitd4v1QyyJeWnlrOcJr5OAJQQJ99CBAC5T7U2XJ3w3AAAYACOGckmt
      - AZURE_SPEECH_REGION=francecentral
      - API_KEY=Maximussuper-4
    ports:
      - "8000:8000"
    volumes:
      - video_outputs:/app/outputs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  video_outputs:
